<!DOCTYPE html>
<html>
<head> 
<title>{% block title %}{{ user.username|capfirst}}'s blog{% endblock %}</title> 
{% load static %}
<link rel='stylesheet' type='text/css' href="{% static 'base_style.css' %}">
</head>
<body>
  <div id="container">
    <div id="menu">
      <br><center><h1>{{ user.username|capfirst}}'s blog</h1></center>
      <div class="navg" align="right">
        <ul><b>[ {{ user.username }} ] </b>
        {% block header %} {% endblock %}
        <a href="/account/logout">Logout</a> 
      </div> 
    </div>
    {% block sidebar %} 
      <div id="sidebar">
        <ul>
        {% for yr, months in archive.items %}
          <li>{{ yr }}</li>
          {% for mnt, titles in months.items %}
          <ul>
            <li>{{ mnt }}  </li>
            {% for ttl in titles %}
            <ul>
              <li><a href="/{{ yr }}/{{ mnt }}/{{ ttl|slugify }}"> {{ ttl }}</a> </li>
            </ul>
            {% endfor %} 
          </ul>
        {% endfor %} 
        </ul> 
        {% endfor %}
          </div>
    {% endblock %}
    <div id="content">{% block content %}{% endblock %}</div>
    <div align="center" id="footer">{% block footer %}Powered by SAP.{% endblock %}</div>
  </div>
</body>
</html>
